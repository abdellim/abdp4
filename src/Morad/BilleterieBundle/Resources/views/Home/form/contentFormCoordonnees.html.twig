<div class="col-md-8" style="opacity: 0.85">
	<div class="well">
    	<h2>Réservation du {{ reservation.date|date('d/m/y') }}</h2><hr>
	</div>
    {% set i = 0 %}
	{{ form_start(formCordonnees) }} 
	{% for coordonneesField in formCordonnees.coordonnees %}
		{% set i = i+1 %}
		<div class="well">
			<h3>Participant {{ i }}</h3>
	        {{ form_errors(coordonneesField) }}
	        {{ form_widget(coordonneesField) }}
	    </div>
	{% endfor %}
	{{ form_rest(formCordonnees) }}
	{{ form_end(formCordonnees) }}
</div>

  



